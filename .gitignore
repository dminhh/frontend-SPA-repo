# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
*.tsbuildinfo

# Vite env files (VITE_API_BASE, and anything with secrets a future .env might
# hold) — *.local already covers .env.local, but plain .env is not a *.local
# match and must be listed explicitly.
.env
.env.production

# Agent tooling scratch, not part of the project
.claude/settings.local.json
.superpowers/

# Design docs kept local, not published
docs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
